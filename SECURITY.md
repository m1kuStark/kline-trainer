# 安全策略

## 支持的版本

当前仅支持最新发布版本（v0.3.0 及其后续修补版本）。旧版本不单独维护，请在升级后复测。

## 信任模型

训练器按"单用户、纯本机"设计：

- 生产模式只监听本机回环地址 `127.0.0.1`，**没有登录鉴权**。不要把端口暴露到局域网或公网（例如通过反向代理或端口转发），也不要把监听地址改为 `0.0.0.0` 对外开放——任何能访问该端口的人都可以读写你的训练数据。
- 程序只读取通达信目录，不修改、不上传其中的文件；不联网下载行情，无遥测、无自动更新。
- 训练数据库（默认 `%USERPROFILE%\.a-share-kline-trainer\trainer.sqlite`）和浏览器中的训练录像包含你的交易记录。导出分享录像、反馈问题时请自行确认不含不想公开的内容。

如果你发现的问题仅在你主动把服务暴露到网络时才成立，请仍然报告，但请在描述中说明这一前提。

## 如何报告漏洞

**请不要在公开 Issue 中描述可被利用的细节。**

1. 优先使用 GitHub 的私密漏洞报告：仓库页面 **Security → Report a vulnerability**。维护者已启用该功能时，请直接通过它提交完整细节。
2. 若该入口不可用，请开一个**不包含任何漏洞细节**的公开 Issue，内容只需请维护者启用私密漏洞报告（或提供其他私密联系方式）；等私密渠道可用后，再在其中提交细节。在此之前请自行妥善保管细节，不要在公开渠道补发。

报告请包含：影响的版本与下载方式（Releases 包 / 源码运行）、操作系统、复现步骤或概念验证、影响评估。不要附上你的训练数据库、真实交易数据或通达信目录内容。

## 处理范围与响应

- 范围：本仓库的代码与官方发布包。
- 不在范围：通达信软件及其数据文件（向其官方渠道反馈）；第三方依赖自身的问题（可同时向对应上游报告，并在Issue中告知我们）。
- 本项目由维护者以尽力而为方式处理，暂无固定响应时限承诺；确认后会尽快发布修复版本并致谢报告者（除非你要求匿名）。
